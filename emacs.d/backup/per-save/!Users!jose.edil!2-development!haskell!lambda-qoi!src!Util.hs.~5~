
module Util where

import Data.Word
import qualified Data.ByteString.Lazy as BS

infix 4 !
(!) :: BS.ByteString -> Int -> Word8
(!) = BS.index

infix 8 .>>., .<<.
(.>>.), (.<<.) :: Bits a => a -> Int a
(.>>.) = shiftR
(.<<.) = shitfL
